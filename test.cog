Def List as (
    Def F;
    Let S be Stack;
    Clear;
    Let L be Stack F;
    For each in S do (); ~~ put the stack back
    L ~~ put the list on the stack
);

Print Split on " " with "Hello world!";